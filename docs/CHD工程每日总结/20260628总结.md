# 20260628 CHD 工程每日总结

## 一句话状态

今天把 IM-LT 正式评估口径和 metrics 模块命名收齐：native 默认不再走简化映射，AHD/CHD 在线评估统一到 Powerlaw_500 的 1024 live-edge worlds 与 1024 RR sets；同时按任务前缀拆分 `ND_*` / `IM_*` metrics，并删除旧 `metrics.fragmentation` 兼容 shim。

## 今日主线

1. 预先修正 IM 12 图 benchmark 的默认评估口径，避免 full LLM 大实验启动后混入 smoke/debug 逻辑。
2. 将 native IM-LT 默认切到 formal 4096 common-worlds：默认读取已完成的 `LT_RandomThreshold / 4096 / base_seed=20260626` native 表。
3. 将 AHD/CHD 的在线 IM 评估默认统一为 Powerlaw_500 上的 `1024` 次固定 IC live-edge worlds 和 `1024` 个 RR sets。
4. 在 CHD 输出 manifest 中补齐 IM 任务配置，明确 root、candidate interface、online graph、p、base seed、预算和 ranking formula。
5. 将 metrics 模块按任务前缀重命名和拆分：ND fragmentation、IM LT evaluator、IM IC/RR evaluator 分离。
6. 删除旧 `src/metrics/fragmentation.py`，源码入口全部改用 `metrics.ND_fragmentation`。

## 关键代码变更

- `src/scripts/run_im_12graph_benchmark.py`
  - `--online-live-edge-worlds` 默认改为 `1024`。
  - `--online-rr-sets` 默认改为 `1024`。
  - 新增 `--native-eval-mode formal4096|debug-simple`，默认 `formal4096`。
  - 新增 `--native-source-run-dir`，默认指向 `src/runs/20260626-020000-IM-IM-lt-common-worlds-4096`。

- `src/experiments/im_12graph_benchmark.py`
  - formal native 模式默认读取已完成的 native 4096 LT common-worlds 结果。
  - old simplified `NATIVE_METHODS` 仅保留给 `debug-simple`。
  - manifest 记录 `native_eval_mode`、`native_source_run_dir`、`native_model_type=LT_RandomThreshold`、`native_simulations=4096`、`native_base_seed=20260626`。
  - CHD IM manifest 补全 `task=im`、`root=DegreeDiscountIC`、`candidate_interface=def seed_order(G, k)`、`online_graph=network/12main_network/Powerlaw_500.edgelist`、`online_live_edge_worlds=1024`、`online_rr_sets=1024`、`p=0.1`、`base_seed=20260626`、full run 预算 `stage1=300, stage2=10, stage3=200`，以及 ranking formula。

- `src/model/stage1_stage3_search.py`
  - IM 默认 `online_live_edge_worlds=1024`、`online_rr_sets=1024`。
  - IM 默认 online graph 指向 `network/12main_network/Powerlaw_500.edgelist`。
  - prepare manifest 中补充 IM online graph、p、base seed 和在线评估规模。

- `src/experiments/chd_main_search.py`
  - CLI 默认 online graph 同步为 Powerlaw_500。
  - CLI 默认 live-edge worlds / RR sets 同步为 `1024 / 1024`。
  - `task=im` 时写入完整 `im_config`。

- `src/metrics/ND_fragmentation.py`
  - 作为 ND fragmentation canonical 模块保留。
  - 所有活跃源码中的旧 `metrics.fragmentation` 导入已替换为 `metrics.ND_fragmentation`。

- `src/metrics/IM_LT_evaluator.py`
  - 承接 LT random-threshold common-worlds evaluator。
  - 提供 `LTSpreadResult`、`normalize_seed_order`、`evaluate_lt_spread`、`evaluate_lt_spread_many`。

- `src/metrics/IM_IC_evaluator.py`
  - 承接 IM 在线 IC/RR proxy evaluator。
  - 提供 `build_fixed_live_edge_worlds`、`build_fixed_rr_sets`、`fixed_live_edge_spread`、`rr_coverage`。

- `src/model/im_task.py`
  - 保留 CHD IM task adapter、DegreeDiscountIC root、prompt/ranking/final selection。
  - 纯 IC/RR evaluator 改从 `metrics.IM_IC_evaluator` 导入。

- `src/model/im_lt_evaluator.py`
  - 改为兼容 wrapper，转发到 `metrics.IM_LT_evaluator`。
  - 新代码不再直接依赖该旧位置。

- `src/metrics/__init__.py`
  - 统一导出 canonical ND 和 IM metric/evaluator 符号。

## 验证结果

- canonical import 已验证：
  - `from metrics.ND_fragmentation import compute_metrics`
  - `from metrics.IM_LT_evaluator import evaluate_lt_spread_many`
  - `from metrics.IM_IC_evaluator import fixed_live_edge_spread, rr_coverage`

- compatibility wrapper 已验证：
  - `model.im_lt_evaluator.evaluate_lt_spread` 与 `metrics.IM_LT_evaluator.evaluate_lt_spread` 指向同一 callable。

- 测试状态：
  - `src/tests/test_im_task_chd_integration.py`
  - `src/tests/test_im_native_baselines.py`
  - `src/tests/test_im_12graph_benchmark_config.py`
  - `src/tests/test_stage3_full_validation_contract.py`

- 编译检查覆盖 metrics、IM benchmark/scripts、IM task adapter、ND full-validation 相关入口。

## 已确认的注意事项

- `src/metrics/fragmentation.py` 已删除；以后不再支持 `from metrics.fragmentation import ...`。
- 旧 notebook 或临时脚本如需复跑，应改成 `from metrics.ND_fragmentation import ...`。
- `CHD2026` 本地工作目录仍不是 git 仓库，GitHub 上传继续通过 `.codex_tmp/CHD2026_github_mirror` 镜像仓库完成。
- GitHub 镜像只同步代码与文档，不同步 `network/`、`src/runs/`、`artifacts/`、本地缓存、虚拟环境或 API key。

## 下一步建议

1. 启动 full LLM 前，先再次确认 `run_im_12graph_benchmark.py --include native` 默认不会进入 debug-simple native。
2. full LLM 完成后，用 `run_im_lt_common_worlds_from_existing.py --simulations 4096 --base-seed 20260626` 重评估 AHD/CHD final seeds。
3. 最终报告只合并 formal native 4096 LT rows 与新 AHD/CHD 4096 LT common-worlds rows，避免混用 smoke effective simulations。
