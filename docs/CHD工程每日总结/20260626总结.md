# 20260626 CHD 工程每日总结

## 一句话状态

今天把 CHD 从 ND 主任务最小侵入扩展到 IM 任务，完成了真实 LLM 小预算 smoke、native/AHD/CHD 三类方法的 12 图 IC/LT 汇总，并在午夜后补跑了 native LT common-worlds 4096 次随机阈值重评估。

## 今日主线

1. 解析并执行 `src/runs/20260626-004712-IM-改造CHD并运行CHD-IM的计划` 下的两个计划。
2. 在不重构 ND 主流程的前提下，为 `stage1_stage3_search.py` 增加 `task=im` 分支。
3. 新增 IM 在线评估、12 图 benchmark、LT common-world evaluator 和 LLM 容量审计脚本。
4. 完成真实 LLM smoke：AHD 9 方法各 1 个候选，CHD 使用 `3+1+3` 小预算。
5. 确认 native baseline 结果先完成，再逐条审计 LLM raw/error，未发现“模型已满/请换模型/overloaded/capacity”等问题。

## 关键代码变更

- `src/model/candidate.py`
  - `extract_code`、`validate_code`、`make_program`、`compile_candidate` 增加 `function_name` 参数。
  - 默认仍为 `degree_order`，ND 旧行为不变。
  - IM 显式支持 `seed_order(G, k)`。

- `src/model/im_task.py`
  - 新增 CHD-IM task adapter。
  - 包含 DegreeDiscount-IC root、固定 IC live-edge worlds、固定 RR sets、IM candidate evaluation、CHD IM rank formula、final selection。
  - CHD IM 在线 score：
    `0.7*rank(spread_ic)+0.1*rank(relative_spread_ic)+0.1*rank(rr_coverage)+0.1*rank(time_s lower better)`。

- `src/model/stage1_stage3_search.py`
  - 新增 `StageSearchConfig.task`，默认 `nd`。
  - `--task im` 时切换 root、prompt、candidate interface、evaluation、ranking、Stage2 summary、Stage3 final selection。
  - IM final candidates 写入 `chd/final_candidates/CHD-Final-Best.py` 和 `CHD-Final-Fast.py`。

- `src/experiments/chd_main_search.py`
  - CLI 新增：
    - `--task nd|im`
    - `--online-graph`
    - `--online-live-edge-worlds`
    - `--online-rr-sets`

- `src/experiments/im_12graph_benchmark.py`
  - 新增统一 IM 12 图 benchmark。
  - 支持 native、AHD、CHD 三类方法。
  - smoke 模式会把最终 eval simulations cap 到较小值，并在 manifest 中记录 requested/effective。

- `src/scripts/run_im_12graph_benchmark.py`
  - 新增统一 benchmark 命令入口。
  - 支持 `--key-file`，只读入环境变量，不保存 key。

- `src/scripts/audit_llm_capacity.py`
  - 新增 LLM 容量/换模型错误审计脚本。
  - 扫描 raw LLM、call summary 和 candidate log。

- `src/model/llm.py`
  - system prompt 改为尊重用户 prompt 中的候选接口，不再写死 `degree_order(G)`。
  - 新增 `HAST_LLM_FALLBACK_MODELS` 支持，遇到模型容量类错误可自动尝试备用模型。

- `src/model/im_lt_evaluator.py`
  - 新增 LT random-threshold common-world evaluator。
  - 每个 node 每次 simulation 采样 `Uniform(0,1)` threshold。
  - 无向无权图中，激活邻居对节点 `v` 的压力为 `1/degree(v)`。

- `src/scripts/run_im_lt_common_worlds_from_existing.py`
  - 从已有 seed 表重评估 LT common worlds。
  - 今天凌晨用它跑完 native LT 4096 次模拟。

- `src/scripts/run_native_lt_ndlib_parallel.py`
  - 为 native IM 启发式准备的并行/超时隔离 runner。
  - 用子进程隔离慢方法，避免一个慢方法拖死全局。

## 关键实验产物

### 真实 LLM smoke

目录：

`src/runs/20260626-011940-IM-im12-real-smoke`

状态：

- AHD 9 方法各生成 1 个真实 LLM 候选，9/9 valid。
- CHD 完成 `stage1=3, stage2=1, stage3=3`。
- CHD final candidates 已生成。
- 12 图 summary 已补齐。
- native baseline 专用结果已补写：
  - `native/per_graph_metrics.csv`
  - `native/method_mean_metrics.csv`
- LLM 容量审计：
  - `llm_call_records_scanned=16`
  - `capacity_or_switch_model_issues=0`
  - `api_key_saved=false`

重要文件：

- `summary/all_methods_ic_mean.csv`
- `summary/all_methods_lt_mean.csv`
- `summary/all_methods_ic_per_graph.csv`
- `summary/all_methods_lt_per_graph.csv`
- `native/per_graph_metrics.csv`
- `native/method_mean_metrics.csv`
- `llm_capacity_audit.json`

### Native LT common-worlds 4096

目录：

`src/runs/20260626-020000-IM-IM-lt-common-worlds-4096`

状态：

- started_at: `2026-06-27 00:00:54`
- completed_at: `2026-06-27 00:02:25`
- simulations: `4096`
- per_graph_rows: `108`
- valid_rows: `75`
- skipped_rows: `33`
- disabled_methods: `MIA-PMIA-family`

结果速记，按 `mean_normalized_spread`：

1. `ClusterGreedy-LT-2024`: `0.4393`, `valid_rate=0.25`, 强但只在少量图有效且很慢。
2. `DomIM-2021`: `0.2525`, `valid_rate=0.9167`。
3. `DegreeDiscountIC`: `0.2434`, `valid_rate=1.0`, 速度稳定。
4. `MCGreedy`: `0.2413`, `valid_rate=0.4167`。
5. `CELF++`: `0.1457`。
6. `CELF`: `0.1417`。
7. `IMM-style`: `0.1219`。
8. `RRGreedy`: `0.1110`。
9. `MIA-PMIA-family`: skipped/disabled。

## 已确认的坑和处理

- `CHD2026` 不是 git 仓库，所有改动和结果只能按文件时间/路径追踪。
- `gh` 未安装，不能使用 GitHub CLI PR 流程；后续上传用普通 `git`。
- 真实 smoke 原始命令超时不是 LLM 未完成，而是最后 12 图汇总阶段慢；实际 AHD 和 CHD LLM 阶段都已完成。
- native LT 有些方法很慢或 valid rate 低，尤其 `ClusterGreedy-LT-2024`、`MCGreedy`、`CELF/CELF++`；比较时必须同时报告 `valid_rate` 和 `mean_time_s`。
- `MIA-PMIA-family` 在 4096 common-worlds 跑法中被 disabled，不应误读为空结果。
- smoke 里的 IC/LT summary 有 cap 过的 effective simulations；论文级结论应优先看 4096 common-worlds 或 full run。

## 明天早上继续的最短路径

1. 先打开：
   - `src/runs/20260626-011940-IM-im12-real-smoke/run_manifest.json`
   - `src/runs/20260626-020000-IM-IM-lt-common-worlds-4096/run_manifest.json`

2. 先确认 native 已完成：
   - `src/runs/20260626-011940-IM-im12-real-smoke/native/method_mean_metrics.csv`
   - `src/runs/20260626-020000-IM-IM-lt-common-worlds-4096/native/method_mean_metrics.csv`

3. 如果继续公平 LT 对比，下一步应把 AHD/CHD final seed sets 也放到同一个 `LT_RandomThreshold common-worlds 4096` 评估器里重评估。

4. 如果继续 LLM 运行，先跑容量审计：

```powershell
& 'E:\my_evns\py312_torch28\python.exe' src/scripts/audit_llm_capacity.py `
  --run-dir src/runs/20260626-011940-IM-im12-real-smoke
```

5. 若要启用备用模型，设置：

```powershell
$env:HAST_LLM_FALLBACK_MODELS='model_a,model_b'
```

## 推荐下一步

优先补一个 `run_im_lt_common_worlds_from_existing.py` 的全方法版本，让 native、AHD、CHD 使用同一批 4096 LT threshold worlds。这样明天就能把 smoke 表从“管线验证”推进到“可分析的公平 LT 对比”。
